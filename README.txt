#This program makes use of Javascript, HTML and CSS.
#It's a program for a game whereby a card is clicked to reveal the number hidden (On the other side). Afterwards the user clicks another( second card)
# and tries to find a match to the first.
#In the case there is success, the pair are removed from play, otherwise the user is given another chance to play and try to match
#The player wins, when all pairs have been matched successfully